import React from 'react';

const APINinjasHome = () => {

  return (
      <div>
          <h1>Welcome!</h1>
        <div>
        This project was built to consume the APINinjas API and all of its parameters in a user friendly way.
        Pick from the above tabs to explore its different APIs, change parameters, and view their results.
        </div>
      </div>
  );
};

export default APINinjasHome;
